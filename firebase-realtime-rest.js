// Thin wrapper re-exporting the helper from `src/firebase` so there's a single source of truth.
export { fetchFromRealtimeRest, fetchProjectsAsArray } from './src/firebase/firebase-realtime-rest.js';
